<!--コメント投稿フォーム-->
<div>
<%= form_with model: [bucket_list, new_comment], id: "js_form", local: false do |f| %><!--form_withは『local: fales』-->
  <div class="form-group">
    <%= f.text_area :comment, rows: '4', class: 'form-control' %>
  </div>
  <%= f.submit "送信" %>
<% end %>
</div>