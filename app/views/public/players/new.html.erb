<h3>プレイヤー登録</h3>
<%= form_with model: @new_player, local: true do |f| %>
  <div class="form-group">
    <%= f.label :player_name, "プレイヤー名" %>
    <%= f.text_field :player_name %>
  </div>
  <div class="form-group">
  <%= f.submit "登録", class: "btn btn-primary" %>
  </div>
<% end %>